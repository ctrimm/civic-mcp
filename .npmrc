# pnpm settings
# Hoist packages needed by the extension build tools (vite / crxjs plugins
# expect node_modules at the workspace root).
shamefully-hoist=false
public-hoist-pattern[]=*typescript*
public-hoist-pattern[]=*eslint*
public-hoist-pattern[]=*prettier*
